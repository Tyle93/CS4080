#include <string>
#include <iostream>
#include "Matrix.h"

int main() {
	/*
	int intArray[3][5] = { {1, 2, 4, 4},{5, 5, 4, 2 },{3, 1, 1, 5 } };
	Matrix<int> m(intArray);
	m.getMax();

	std::string stringArray[3][5] = {{"David", "Kelin", "Peter", "Zag", "Diana"},{"Elin", "Adam", "Young", "Peter", "Zag"}};
	Matrix<std::string> s(stringArray);
	s.getMax();

	std::cout << s[0][1] << std::endl;
	*/

	Matrix<std::string> s = 
	s.getMax();
	
}